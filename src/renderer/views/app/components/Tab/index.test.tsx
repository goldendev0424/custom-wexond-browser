import { onMouseOver, onMouseLeave } from './index';

describe('Test that the user hovers on a tab', () => {
  test('renders the page corresponding to that tab', () => {
    // expect(onMouseOver).toBe(null);
  });

  test('the original content of tab is desplayed', () => {
    // expect(onMouseLeave).toBe(null);
  });
  
});